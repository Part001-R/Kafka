Learning Kafka.

Kafka topic - `test`

Producer - transmits data to three Kafka partitions (0, 1, 2)
Consumer - monitors the appearance of new data in partition 1.

+ `v0.0.1` - Producer, Consumer.
